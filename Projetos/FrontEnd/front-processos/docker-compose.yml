version: '3.8'

services:
  app:
    container_name: dockerized-react-app
    image: node:12
    ports:
      - '3000:3000'
    volumes:
      - ./:/srv/react-docker
    working_dir: /srv/react-docker
    command: 'npm start'
    environment:
      - NODE_ENV=development